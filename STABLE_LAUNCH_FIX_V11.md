# STABLE_LAUNCH_FIX_V11

Исправлено:
- убран автозапрос Telegram requestFullscreen на старте, который мог провоцировать повторную инициализацию WebView;
- старт больше не восстанавливает опасные страницы `view`, `seller`, `profileAds` из старой sessionStorage;
- добавлен жёсткий fallback на главную, если boot застрял;
- добавлен fallback-рендер для `view` без объявления, чтобы не было чёрного экрана;
- возвращено старое поведение полоски загрузки без "догоняющего" интервала;
- сохранены безопасные верхние отступы для Telegram fullscreen без провала страниц вниз.

Деплой:
- нужен redeploy на Vercel;
- Render не нужен.
