# LAYOUT_FIX_V7

Исправлено:
- экран «Создать» вернул нормальный верхний отступ;
- на ПК/планшетах приложение больше не зажимается в мобильную ширину 430px;
- страницы адаптируются под ширину экрана через красивые контейнеры;
- карточки и кнопки на больших экранах не должны обрезаться;
- в разделах профиля убрана синяя/бирюзовая подсветка внутри карточек объявлений;
- кнопки «Открыть», «Вернуть», «Продвинуть», «Снять» отделены от карточки объявления отдельным блоком.

Деплой:
- нужен redeploy на Vercel;
- Render не нужен.
